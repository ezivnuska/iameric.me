import React, { useContext } from 'react'
import {
    DestroyForm,
} from '.'
// import { AppContext } from '../AppContext'

export default () => {

    // const {
    //     dispatch,
    //     user,
    // } = useContext(AppContext)
    
    return (
        <DestroyForm />
    )
}