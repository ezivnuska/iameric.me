import ModalView from './ModalView'
import {
    ModalContext,
    ModalContextProvider,
    useModal,
} from './ModalContext'

export default ModalView
export {
    ModalContext,
    ModalContextProvider,
    useModal,
}