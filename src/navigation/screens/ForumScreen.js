import React from 'react'
import {
    EntryModule,
    ScreenTitle,
    Screen,
} from '@components'

export default () => (
    <Screen>
        <ScreenTitle title='Forum' />
        <EntryModule />
    </Screen>
)