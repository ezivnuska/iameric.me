import React from 'react'
import {
    ProductModule,
} from '@components'
import {
    Screen,
} from '.'

export default () => (
    <Screen title='Products'>
        <ProductModule />
    </Screen>
)