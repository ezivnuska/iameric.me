import React from 'react'
import {
    ProductModule,
    Screen,
} from '@components'

export default ({ navigation }) => (
    <Screen navigation={navigation}>
        <ProductModule />
    </Screen>
)