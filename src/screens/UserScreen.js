import React from 'react'
import { Screen } from '../components'

const UserScreen = props => {
    return (
        <Screen { ...props }>
            
        </Screen>
    )
}

export default UserScreen