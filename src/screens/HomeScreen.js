import React from 'react'
import {
    Screen,
    ThemedText,
} from '@components'

export default ({ navigation }) => {
    
    return (
        <Screen>
            <ThemedText>Welcome Home</ThemedText>
        </Screen>
    )
}