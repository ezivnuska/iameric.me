import React from 'react'
import {
    Text,
    View,
} from 'react-native'
import {
    Screen,
} from '@components'

export default ({ navigation }) => {
    
    return (
        <Screen>
            <Text>Welcome Home</Text>
        </Screen>
    )
}