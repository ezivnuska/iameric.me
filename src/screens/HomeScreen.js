import React from 'react'
import {
    Screen,
    ThemedText,
} from '@components'

export default ({ navigation }) => {
    
    return (
        <Screen title='Home'>
            <ThemedText>Welcome</ThemedText>
        </Screen>
    )
}