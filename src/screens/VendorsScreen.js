import React from 'react'
import {
    Screen,
    VendorModule,
} from '@components'

export default ({ navigation }) => (
    <Screen navigation={navigation}>
        <VendorModule />
    </Screen>
)