import React from 'react'
import {
    Screen,
    DriverModule,
} from '@components'

export default ({ navigation }) => (
    <Screen navigation={navigation}>
        <DriverModule />
    </Screen>
)