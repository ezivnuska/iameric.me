import React from 'react'
import {
    EntryModule,
    Screen,
} from '../components'

export default ({ navigation }) => (
    <Screen navigation={navigation}>
        <EntryModule />
    </Screen>
)