const createBip = require('./createBip')
const deleteBip = require('./deleteBip')
const getBip = require('./getBip')
const getBips = require('./getBips')

module.exports = {
    createBip,
    deleteBip,
    getBip,
    getBips,
}